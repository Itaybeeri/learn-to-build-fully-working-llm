# Learner's private progress — never tracked, never in a PR.
# This is your personal state; it stays local to your clone.
.progress/

# Trained model weights (regenerated by running the demos — don't track binaries)
*.pt
*.npz

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs / scratch
*.log
tmp/

claude-workspace.code-workspace
